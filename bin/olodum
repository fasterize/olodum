#!/usr/bin/env node
var domain = process.argv[2];
var target = process.argv[3];
var olodum = require('../lib/olodum');

domain = !domain ? '' : domain;
target = target || '127.0.0.1';

olodum.init(domain, target);

